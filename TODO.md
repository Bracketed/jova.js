# @TODO

- Finish updating/redoing documentation (http/ContentTypes.ts)
- Change version to major (2.0)
- Update `README.md`
- Update the Route Utilities to actually work with stuff
- Rollback handlers to parse the `container` field
- Add new resource config for the initialiser
    - `.jovarc.yml`
    - `.jovarc.yaml`
    - `.jovarc.json`
    - `.jovarc.jsonc`
    - `jova.config.ts`
    - `jova.config.js`
    - `.jovarc.env`
    - `.jovarc.local.env`
    - `jova` field in `package.json`
- Add an option in the initialiser to define the location of the resource config above, default should be at the package.json root ^
